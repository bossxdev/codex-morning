services:
  codex-morning:
    build: .
    volumes:
      - .:/workspace
    command: daemon
    environment:
      - CLAUDE_MORNING_CRON_SCHEDULE=0 0 * * *,0 5 * * *,0 10 * * *,0 15 * * *,0 20 * * *
      - TZ=Asia/Bangkok
    restart: unless-stopped
