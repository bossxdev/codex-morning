services:
  codex-morning:
    build: .
    volumes:
      - codex-auth:/root/.codex
    command: daemon
    tty: true
    environment:
      CODEX_MORNING_CRON_SCHEDULE: "0 */5 * * *"
      TZ: Asia/Bangkok
    restart: unless-stopped

volumes:
  codex-auth:
